name: PR

on:
  pull_request:

permissions:
  actions: read
  contents: read
  pull-requests: read

jobs:
  ci:
    # Pin: #13457 merge — restore-only Rust dependency cache on the PR runner lane.
    uses: paperclipai/paperclip/.github/workflows/pr-trusted.yml@5b913e794315530b95f2bc95dd80e3ebc266b257
